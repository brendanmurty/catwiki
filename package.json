{
  "name": "catwiki",
  "version": "1.0.0",
  "description": "",
  "main": "server/index.js",
  "scripts": {
    "build": "cd client && npm run build",
    "start": "npm run build && node server/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Brendan Murty <brendan@murty.au> (https://murty.au)",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.17.2"
  },
  "engines": {
    "node": "v17.4.0"
  }
}
